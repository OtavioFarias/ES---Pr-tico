package classes;

import java.util.Scanner;
import classes.*;

public class Cadastro {

    public static void cadastrarArtigo(){



    }

    public static void cadastrarAtividadeComplementar(){



    }

    public static void cadastrarCadeiraObrigatoria(){



    }

    public static void cadastrarCadeiraOpciona(){



    }

    public static void cadastrarEstagio(){



    }

    public static void cadastrarPraticaExtensionista(){



    }

    public static void cadastrarResumoExpandido(){



    }

    public static void cadastrarServicoComunitario(){



    }

    public static void cadastrarENADE(){



    }
}


        /*Scanner scanner = new Scanner(System.in);

        System.out.print("\nNome: ");
        String nome = scanner.nextLine();

        System.out.print("\nMatrícula: ");
        int matricula = scanner.nextInt();



     //   scanner.nextLine(); // limpar o buffer após nextInt

        // Cadastrar cadeiras realizadas
        while (true) {
            System.out.print("\nDeseja cadastrar uma cadeira já realizada (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Nome da cadeira: ");
            String nomeCadeira = scanner.nextLine();
            // adicionar lógica para armazenar a cadeira
        }

        // Cadastrar cadeiras finalizadas
        while (true) {
            System.out.print("\nDeseja cadastrar uma cadeira já finalizada (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Nome da cadeira finalizada: ");
            String nomeCadeiraJaFeita = scanner.nextLine();
            // adicionar lógica para armazenar
        }

        // Cadastrar artigos
        while (true) {
            System.out.print("\nDeseja cadastrar um artigo (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Título: ");
            String titulo1 = scanner.nextLine();

            System.out.print("Veículo: ");
            String veiculo = scanner.nextLine();

            System.out.print("Semestre: ");
            int semestre1 = scanner.nextInt();
            scanner.nextLine();

            // adicionar lógica
        }

        // Atividades Complementares
        while (true) {
            System.out.print("\nDeseja cadastrar uma Atividade Complementar (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Descrição: ");
            String descricao = scanner.nextLine();

            System.out.print("Horas: ");
            int horas1 = scanner.nextInt();
            scanner.nextLine();
        }

        // Estágios
        while (true) {
            System.out.print("\nDeseja cadastrar um estágio (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Empresa: ");
            String empresa = scanner.nextLine();

            System.out.print("Horas: ");
            int horas2 = scanner.nextInt();
            scanner.nextLine();

            System.out.print("Tipo: ");
            String tipo = scanner.nextLine();
        }

        // Prática Extensionista
        while (true) {
            System.out.print("\nDeseja cadastrar uma prática extensionista (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Projeto: ");
            String projeto = scanner.nextLine();

            System.out.print("Papel: ");
            String papel = scanner.nextLine();

            System.out.print("Créditos: ");
            int creditos2 = scanner.nextInt();
            scanner.nextLine();
        }

        // Resumo Expandido
        while (true) {
            System.out.print("\nDeseja cadastrar um resumo expandido (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Título: ");
            String titulo2 = scanner.nextLine();

            System.out.print("Evento: ");
            String evento = scanner.nextLine();

            System.out.print("Semestre: ");
            int semestre3 = scanner.nextInt();
            scanner.nextLine();
        }

        // Serviço Comunitário
        while (true) {
            System.out.print("\nDeseja cadastrar um Serviço Comunitário (1-sim 0-não): ");
            int escolha = scanner.nextInt();
            scanner.nextLine();

            if (escolha != 1) break;

            System.out.print("Atividade: ");
            String atividade = scanner.nextLine();

            System.out.print("Horas: ");
            int horas3 = scanner.nextInt();
            scanner.nextLine();
        }



    public static Historico cadastrarHistorico() {
        return new Historico(); // lógica real deve ser adicionada aqui
    }*/

